# OpenSearch Security - Allowlist (formerly Whitelist)
#
# Controls which REST API endpoints are accessible.
# Disabled here — all authenticated users access endpoints per their roles.
#
# Authors: Balaji Rajan and Claude (Anthropic)
# License: Apache 2.0

_meta:
  type: "allowlist"
  config_version: 2

config:
  enabled: false
  requests: {}
